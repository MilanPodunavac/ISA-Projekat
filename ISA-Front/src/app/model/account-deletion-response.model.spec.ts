import { AccountDeletionResponse } from './account-deletion-response.model';

describe('AccountDeletionResponse', () => {
  it('should create an instance', () => {
    expect(new AccountDeletionResponse()).toBeTruthy();
  });
});
