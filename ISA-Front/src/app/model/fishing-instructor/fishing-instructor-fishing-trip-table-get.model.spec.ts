import { FishingInstructorFishingTripTableGet } from './fishing-instructor-fishing-trip-table-get.model';

describe('FishingInstructorFishingTripTableGet', () => {
  it('should create an instance', () => {
    expect(new FishingInstructorFishingTripTableGet()).toBeTruthy();
  });
});
