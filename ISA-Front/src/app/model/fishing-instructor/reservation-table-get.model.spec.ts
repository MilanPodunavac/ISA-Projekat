import { ReservationTableGet } from './reservation-table-get.model';

describe('ReservationTableGet', () => {
  it('should create an instance', () => {
    expect(new ReservationTableGet()).toBeTruthy();
  });
});
