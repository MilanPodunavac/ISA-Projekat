import { FishingInstructorGet } from './fishing-instructor-get';

describe('FishingInstructorGet', () => {
  it('should create an instance', () => {
    expect(new FishingInstructorGet()).toBeTruthy();
  });
});
