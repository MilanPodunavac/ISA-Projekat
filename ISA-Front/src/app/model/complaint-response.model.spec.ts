import { ComplaintResponse } from './complaint-response.model';

describe('ComplaintResponse', () => {
  it('should create an instance', () => {
    expect(new ComplaintResponse()).toBeTruthy();
  });
});
