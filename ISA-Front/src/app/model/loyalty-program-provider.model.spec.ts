import { LoyaltyProgramProvider } from './loyalty-program-provider.model';

describe('LoyaltyProgramProvider', () => {
  it('should create an instance', () => {
    expect(new LoyaltyProgramProvider()).toBeTruthy();
  });
});
