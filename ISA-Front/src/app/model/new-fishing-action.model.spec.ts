import { NewFishingAction } from './new-fishing-action.model';

describe('NewFishingAction', () => {
  it('should create an instance', () => {
    expect(new NewFishingAction()).toBeTruthy();
  });
});
