import { AccountDeletionRequestGet } from './account-deletion-request-get.model';

describe('AccountDeletionRequestGet', () => {
  it('should create an instance', () => {
    expect(new AccountDeletionRequestGet()).toBeTruthy();
  });
});
