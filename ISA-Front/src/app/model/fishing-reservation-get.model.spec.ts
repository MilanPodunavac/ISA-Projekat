import { FishingReservationGet } from './fishing-reservation-get.model';

describe('FishingReservationGet', () => {
  it('should create an instance', () => {
    expect(new FishingReservationGet()).toBeTruthy();
  });
});
