import { ReviewFishingTripGet } from './review-fishing-trip-get.model';

describe('ReviewFishingTripGet', () => {
  it('should create an instance', () => {
    expect(new ReviewFishingTripGet()).toBeTruthy();
  });
});
