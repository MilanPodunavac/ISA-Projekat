import { LoyaltyProgramClient } from './loyalty-program-client.model';

describe('LoyaltyProgramClient', () => {
  it('should create an instance', () => {
    expect(new LoyaltyProgramClient()).toBeTruthy();
  });
});
