import { ProfitInInterval } from './profit-in-interval.model';

describe('ProfitInInterval', () => {
  it('should create an instance', () => {
    expect(new ProfitInInterval()).toBeTruthy();
  });
});
