import { ComplaintFishingInstructorGet } from './complaint-fishing-instructor-get.model';

describe('ComplaintFishingInstructorGet', () => {
  it('should create an instance', () => {
    expect(new ComplaintFishingInstructorGet()).toBeTruthy();
  });
});
